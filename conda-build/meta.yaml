{% set pyproject = load_file_data("../pyproject.toml", from_recipe_dir=True) %}
{% set python_spec = "python " ~ pyproject.get("project").get("requires-python") %}

package:
  name: im-pytest
  version: {{ pyproject.get("project").get("version") }}

source:
  git_url: https://github.com/munch-group/im-pytest.git

build:
  number: 0
  script: {{ PYTHON }} -m pip install . -vv
  python_version_independent: true
  # entry_points:
  #  - im-pytest = im_pytest:im_pytest

requirements:
  host:
    - {{ python_spec }}
    - pip
    - setuptools>=65.0
  run:  # your library dependencies
    - {{ python_spec }}
    {% for dep in pyproject.get("project").get("dependencies") %}
    - {{ dep.lower() }}
    {% endfor %}

channels:  # channels for your library dependencies
  - conda-forge

dependencies:   # keep this block with only these packages
  - anaconda-client
  - conda-build>=25.1.3 # from 25 the output file is a .conda not .tar.bz2
  - conda-verify
  - conda-package-handling
  # - conda-forge/label/python_rc::_python_rc # Dummy package to force selection of python_rc channel when channel_priority is set to strict

test:
  imports:
    - im_pytest

about:
  home: https://github.com/munch-group/im-pytest.git
  license: MIT
  dev_url: https://github.com/munch-group/im-pytest.git
  doc_url: https://munch-group.org/im-pytest
